# AGENTS.md

本文件适用于本仓库的 AI 协作者和维护者；子目录中更具体的 `AGENTS.md` 可补充本文件。用户的明确要求优先。

## 项目范围

本仓库是“今日海大吃什么”的贡献服务。它接收匿名投稿、在私有存储中保存待审图片和数据、提供受保护的审核操作，并在批准后将公开内容变更导出为公开站点仓库的 Pull Request。它不提供公开站点运行时数据，也不存放公开快照。

投稿 HTTP 契约在 `contribution-service-handoff.md` 中定义：`POST /v1/submissions` 接受 `metadata` JSON 字段和单张 `image`，只有成功进入待审队列时返回 `202`。服务端必须自行验证元数据、日期、图片真实格式与大小；不得信任扩展名、声明 MIME 类型或客户端校验。

## 架构与安全边界

- 匿名投稿、审核 API、私有数据目录和 PR 凭据均为服务端职责。浏览器和公开站点中不得出现审核或 Git 凭据。
- 待审核的记录和原图只能放在私有数据目录里，不得进入公开 URL、公开快照或日志中的敏感字段。
- 存储键必须由服务端生成，绝不从原始文件名派生。图片在写入前移除可移除的 EXIF 元数据。
- 审核 API 必须进行认证和授权；批准、拒绝、编辑和导出尝试都必须写入可追溯的审核记录，包含审核者、时间和拒绝理由（适用时）。
- CORS 只接受配置的站点 Origin，匿名接口必须限流。不要为跨域匿名投稿开启 Cookie 凭据。
- 公共导出只生成变更计划，由人或 CI 创建站点仓库 PR；不直接写入生产站点分支，并避免重复发布。

## 实现约定

- 优先保持单体、可替换的边界：HTTP 层、校验层、投稿仓储、图片存储、审核服务与导出器应可独立测试。
- 只有一个 Node 运行时和一个数据目录，不引入数据库、对象存储或第二个运行时；数据目录必须位于仓库外的私有位置，并纳入备份。
- 渠道有两条，出口只有一个：投稿服务与 GitHub Issue 表单最终都汇成同一份 `records` 交给导出器，计划生成不允许各写一套。
- API 错误必须使用稳定的 JSON 结构，避免将内部错误、凭据、文件路径或未审内容返回给匿名调用方。
- 使用 UTF-8 和简体中文维护用户可见文案；代码、环境变量和 API 字段保持英文。
- API 契约以 `openapi.yaml` 为准。变更接口时同步更新契约、实现与测试。

## 分支与协作

- 开始前检查 `git status`、当前分支和差异；不覆盖用户已有修改。
- 有远程 `dev` 分支时，从最新 `origin/dev` 创建 `feat/`、`fix/` 或 `chore/` 分支；没有时从最新 `origin/main` 创建。仅本地交付时不需要推送或创建 PR。
- 不重写共享分支历史、强制推送、删除他人的分支或通过重置清除无关改动。
- 一次变更聚焦一个可交付能力；合并前审阅差异，并使用描述实际结果的提交信息。

## 验证

- 使用 Pixi 管理 Node.js 与 pnpm 环境。依赖安装、测试、检查和启动均通过 `pixi run` 调用任务；不得要求维护者安装全局 Node、npm 或 pnpm。
- `package.json` 的 JavaScript 依赖使用 pnpm 管理，提交 `pnpm-lock.yaml`。修改依赖后执行 `pixi run install` 更新锁文件；日常验证使用冻结锁文件。
- 每个行为变更至少包含针对正常路径和失败路径的自动化测试；投稿 API 特别覆盖伪造 MIME、超大文件、非法日期、CORS 和限流。
- 修改后运行仓库声明的测试、静态检查和 `git diff --check`。无法运行时，交付中如实说明命令、原因和已完成检查。
- 不提交依赖缓存、构建产物、测试临时目录、私有上传文件、环境文件或密钥。新增依赖时更新对应锁文件，不手工修改锁文件。
