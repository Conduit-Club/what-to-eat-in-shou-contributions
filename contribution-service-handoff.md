# 贡献服务交接说明

本文用于启动独立的“今日海大吃什么”贡献服务仓库。现有 Docusaurus 站点继续负责公开内容、餐厅卡片和静态构建；投稿接收、图片存储、审核和公开数据导出由新仓库负责。

## 为什么拆分

公开站点应保持纯静态、可离线构建，不能在浏览器或构建环境中持有后台密钥，也不应依赖在线投稿服务才能显示餐厅内容。贡献服务则需要处理上传、审核者权限、私有数据目录和访问控制，两者有不同的安全边界、部署方式和发布节奏。

第一阶段只拆两个仓库：

```text
what-to-eat-in-shou-today
  Docusaurus 公开站点
  审核后的 restaurants.json 快照
  Markdown 餐厅详情和公开图片

what-to-eat-in-shou-contributions
  投稿 API
  待审核数据和图片存储
  管理审核界面或管理 API
  公开数据导出与站点仓库 PR 创建
```

暂不建立独立数据仓库。审核模型和导出格式仍会迭代；只有数据需要独立维护周期，或有多个前端消费者时，再考虑进一步拆分。

## 现有站点契约

站点架构的完整约定位于本仓库的 `architecture.md`。新服务必须兼容以下前端行为：

- 站点通过构建变量 `CONTRIBUTION_API_URL` 配置完整投稿地址；未配置时仅导出投稿信息草稿，不发起网络请求。
- 浏览器对接口发送 `POST multipart/form-data`，不手动指定 `Content-Type`。
- 接口接受一个 `metadata` 字段（JSON 字符串）和一个 `image` 字段（单张图片）。
- 成功接收必须返回 `202 Accepted`，含义仅为进入审核队列，不能表示已经公开。
- 非 `202` 状态及网络或超时错误会由前端保留表单并提示投稿结果未确认。
- 当前发布快照数据模型见 `website/src/types/restaurant.ts`，数据源为 `website/src/data/restaurants.json`。

## 投稿 API v1

建议初始接口为：

```text
POST /v1/submissions
Content-Type: multipart/form-data
```

`metadata` 的 JSON 内容：

```json
{
  "schemaVersion": 1,
  "name": "餐厅名称",
  "category": "on-campus",
  "location": "食堂与档口位置",
  "taste": "个人体验",
  "openingHours": "待补充",
  "visitedAt": "2026-09-16",
  "imageFilename": "photo.webp"
}
```

字段约束：

| 字段 | 规则 |
| --- | --- |
| `schemaVersion` | 当前必须为 `1` |
| `name` | 必填，最多 100 字符 |
| `category` | 必填，仅 `on-campus` 或 `off-campus` |
| `location` | 必填，最多 300 字符 |
| `taste` | 必填，最多 2000 字符；属于个人体验，不应改写为客观结论 |
| `openingHours` | 必填，最多 200 字符；未知时允许为“待补充” |
| `visitedAt` | 必填，合法日期；不可作为核验日期 |
| `imageFilename` | 仅作显示或审计参考，不能用于生成存储路径 |
| `image` | 必填；JPEG、PNG 或 WebP，最大 5 MiB |

返回状态：

| 状态 | 含义 |
| --- | --- |
| `202` | 已进入待审核队列 |
| `400` / `422` | 元数据或图片校验失败 |
| `413` | 图片超过大小上限 |
| `429` | 请求频率受限 |
| `5xx` | 服务端异常；客户端会保留内容，用户需稍后确认后再试 |

可选响应体：`{ "id": "...", "status": "pending" }`。后续应增加幂等键和状态查询；在此之前，超时后的投稿可能已经被接收，审核端需要能按内容和时间检索重复投稿。

## 服务职责与安全要求

服务端必须独立完成校验，不能信任浏览器的限制：

- 校验所有字段长度、枚举和日期格式。
- 检测图片真实格式和实际大小，不能只根据 MIME 类型或文件扩展名判断。
- 移除不需要的 EXIF 元数据，再将图片以服务端生成的键写入私有存储（默认本地目录，或注入的外部实现）。
- 将投稿和照片保留在非公开区域；未审核数据绝不能进入公开快照或公开图片路径。
- 为站点 Origin 配置 CORS 白名单和匿名请求限流；当前投稿不带跨域登录 Cookie。
- 审核者令牌、创建 PR 的凭据和重构建 webhook 密钥均只存在服务端。
- 记录审核操作、审核者、时间和拒绝理由，确保内容变更可追溯。

## 推荐数据流

```text
投稿页面
  -> POST /v1/submissions
  -> 私有存储（待审记录与私有原图，默认本地目录）
  -> 审核者批准、编辑或拒绝
  -> 导出器生成公开数据变更
  -> 在站点仓库创建 PR
  -> 人工复核并合并
  -> Docusaurus 静态构建和发布
```

优先让服务为站点仓库创建 Pull Request，而不是直接修改生产数据或让站点构建时请求服务。这样公开内容拥有 Git 历史、代码审查和回滚能力；即使服务不可用，站点仍可正常构建和访问。

导出器至少应更新：

- `website/src/data/restaurants.json` 中的审核后条目。
- 对应的 `website/docs/on-campus/` 或 `website/docs/off-campus/` 餐厅 Markdown 页面；若为新店，同时维护目录 `index.md`、`website/sidebars.ts`。
- 已批准、可公开使用的图片到 `website/static/img/`，并保留来源或授权所需信息。

公开快照中的日期应明确区分：`visitedAt` 是投稿者用餐日期，`updatedAt` 是审核或核验日期。未知日期和图片应使用 `null`，由站点显示“待补充”。

## 首期技术组成

实现保持单体服务，默认一个进程一个本地数据目录；服务核心只依赖 `src/ports/` 的抽象端口，具体存储实现通过 `src/composition.js` 注入（契约见 `adapter-contract.md`）：

- HTTP API：处理匿名投稿和基本速率限制。
- 默认数据目录：投稿记录、私有原图与审核审计各占一个子目录，状态写在记录里；需要数据库或对象存储时注入外部适配器，接口不变。
- 管理端：审核列表、详情、批准、拒绝与导出状态。
- 导出：生成站点变更计划，由人或 CI 创建 PR；服务本身不直接写生产分支。
- OpenAPI 文档：将 API v1 固化为机器可读契约，并为站点端建立契约测试。

默认实现的生产环境启用 HTTPS、目录备份与日志保留策略；外部实现由适配器仓库负责自己的备份与可用性。原图只存在于私有存储，不进入公开 URL。多实例或大流量不是当前目标；需要共享存储时注入外部实现，接口不变。

## 建议实施顺序

1. 在新仓库编写 `README`、OpenAPI v1 和本文件所述数据模型，先确认服务与站点的边界。
2. 实现投稿接收、服务端校验、私有图片存储和待审记录。
3. 为投稿接口补充单元和集成测试，特别覆盖伪造 MIME、超大文件、非法日期、CORS 与限流。
4. 实现受认证保护的最小审核界面或管理 API，并记录审核审计信息。
5. 实现幂等、导出器和站点仓库 PR 创建；先在测试仓库验证生成变更。
6. 在站点仓库设置 `CONTRIBUTION_API_URL`，运行现有 Playwright 端到端测试，同时针对真实服务增加成功、失败和重复投稿场景。

## 当前实现状态

已完成的部分：投稿接收与服务端校验、私有图片存储、审核 API、受认证保护的审核台（`/admin/`，含待审图片接口）、审核审计、两条投稿渠道、站点变更计划生成与 GitHub PR 创建代码；均有自动化测试覆盖，且不依赖任何第三方运行时包。

仍待完成的部分：真实部署（HTTPS、进程守护、备份与恢复）、发布任务在真实站点仓库上的运行验证、已有餐厅的追加导出，以及部署后的端到端测试。

## 非目标

- 不让 Docusaurus 页面在构建或运行时直接读取私有数据目录。
- 不在前端放置服务端密钥或审核凭据。
- 不把 Artalk 等评论 API 当作结构化投稿和审核服务的替代品。
- 不将待审核数据、图片或临时上传 URL 纳入公开快照。
- 不因服务尚未部署而声称在线投稿、审核或自动发布已经可用。
